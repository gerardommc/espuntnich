% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/euclidPriors.R
\name{euclidPriors}
\alias{euclidPriors}
\title{Learn how to set up priors for a Euclidean distance point process model}
\usage{
euclidPriors(
  cent.mean = NULL,
  cent.prec = NULL,
  tau.min = NULL,
  tau.max = NULL,
  beta.mean = NULL,
  beta.prec = NULL
)
}
\arguments{
\item{cent.mean}{Numeric, with as many elements as covariates in the ppmve model. Must be specified in the same order they appear in "covariate.names".
Each value indicates the likely niche centroid coordinates.}

\item{cent.prec}{Numeric, with as many elements as covariates in the ppmve model. Must be specified in the same order they appear in "covariate.names".
Each value indicates the precision (1/variance) of the niche centroid coordinates.}

\item{tau.min}{Positive numeric, with as many elements as covariates in the ppmve model. Must be specified in the same order they appear in "covariate.names".
Each value indicates the minimum value that precision is likely to have.}

\item{tau.max}{Positive numeric, with as many elements as covariates in the ppmve model. Must be specified in the same order they appear in "covariate.names".
Each value indicates the maximum value that precision is likely to have.}

\item{beta.mean}{A single numeric positive or negative value, inticates the mean of the global intercept.}

\item{beta.prec}{A single numeric positive, inticates the precision of the global intercept.}
}
\description{
Identify the correct structure to specify the priors for a euclidean distance model
}
\examples{
priors <- euclidPriors(cent.mean = rep(0, 4),
                       cent.prec = rep(0.1, 4),
                       tau.min = rep(0.01, 4),
                       tau.max = rep(100, 4),
                       beta.mean = 0,
                       beta.prec = 1.0E-4)
}
