% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppmveAsym.R
\name{ppmveAsym}
\alias{ppmveAsym}
\title{Fit an asymmetric inhomogeneous Poisson Point Process Model using skew multivariate normal constraints}
\usage{
ppmveAsym(
  points = NULL,
  covariates = NULL,
  covariate.names = NULL,
  no.bkgd = 5000,
  bias.data = NULL,
  bias.correction = NULL,
  background.points = NULL,
  samples.data = NULL,
  priors = NULL,
  CovMat = "local",
  Distance = "mahalanobis",
  niter = NULL,
  nburnin = NULL,
  nthin = NULL,
  asCoda = TRUE,
  chains = 2,
  WAIC = TRUE,
  parallel = FALSE,
  cores = NULL,
  seed = 123,
  remove.outer.points = TRUE,
  weight.bias.conf = list(positive = TRUE, kernel = "gaussian", sigma = NULL, varcov =
    NULL, weights = NULL, edge = TRUE, zo.norm = FALSE)
)
}
\arguments{
\item{points}{A two-column data.frame with names x and y for the presence localities.}

\item{covariates}{A SpatRaster or imList object, containing the covariates.}

\item{covariate.names}{A character vector with the names of the covariates to be used in the model.}

\item{no.bkgd}{The number of background points to be drawn from the covariates.}

\item{bias.data}{The data to be used to correct observation bias.}

\item{bias.correction}{A character string with values "background" or "weights", indicating the bias correction method.}

\item{background.points}{A two-column data.frame with the x and y coordinates of background points.}

\item{samples.data}{A list containing three data slots: presence.data, background.data, and area.weights.}

\item{priors}{Informative priors list structured matching asymmetric model constraints.}

\item{CovMat}{A character string with values "local" or "locallocal". Relevant only for "mahalanobis" distance.}

\item{Distance}{A character string with values "mahalanobis" or "euclidean".}

\item{niter}{A numeric value indicating the number of MCMC iterations.}

\item{nburnin}{A numeric value indicating the number of MCMC iterations to be discarded.}

\item{nthin}{A numeric value indicating the interval of MCMC iterations from which posterior values will be sampled.}

\item{asCoda}{Logical, indicating whether the object returned by nimble run is of class coda.}

\item{chains}{A numeric value indicating the number of chains to be run.}

\item{WAIC}{Logical, whether to compute the Watanabe--Akaike information criterion.}

\item{parallel}{Logical, whether to run chains in parallel.}

\item{cores}{Numeric value indicating the number of cores to be used.}

\item{seed}{Numeric, indicating the random seed generator number.}

\item{remove.outer.points}{Logical, used to indicate if points lying outside the study window are going to be removed.}

\item{weight.bias.conf}{A list with configurations for the replaceQAreas function and density.ppm.}
}
\value{
An object of class ppmveAsym and the type of distance used.
}
\description{
Fit a minimum volume ellipsoid or distance constraint with asymmetric skewness parameters as a 
covariate within an inhomogeneous Poisson Point Process Model to develop an Ecological Niche Model.
}
\examples{

r <- system.file("extdata", "ChelsaBio.tif", package = "espuntnich") |> terra::rast() |> scale()

p <- system.file("extdata", "pointsAsym.csv", package = "espuntnich") |> read.csv()

m <- ppmveAsym(points = p,
           covariates = r,
           covariate.names = names(r),
           CovMat = "local",
           Distance = "mahalanobis",
           no.bkgd = 5000,
           niter = 10000,
           nthin = 9,
           nburnin = 1000,
           chains = 1)

}
