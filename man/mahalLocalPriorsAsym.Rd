% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mahalLocalPriorsAsym.R
\name{mahalLocalPriorsAsym}
\alias{mahalLocalPriorsAsym}
\title{Set up priors for a Mahalanobis distance point process model with local background covariance tracking (Asymmetric)}
\usage{
mahalLocalPriorsAsym(
  cent.mean = NULL,
  cent.prec = NULL,
  mu.b.mean = NULL,
  mu.b.prec = NULL,
  R = NULL,
  alpha.mean = NULL,
  alpha.prec = NULL,
  beta.mean = NULL,
  beta.prec = NULL
)
}
\arguments{
\item{cent.mean}{Numeric vector with as many elements as covariates in the model, indicating the likely niche centroid coordinates.}

\item{cent.prec}{Numeric vector with as many elements as covariates in the model, indicating the precision of the niche centroid coordinates.}

\item{mu.b.mean}{Numeric vector with as many elements as covariates in the model, indicating the mean of the background environmental distribution.}

\item{mu.b.prec}{Numeric vector with as many elements as covariates in the model, indicating the precision of the background environmental distribution.}

\item{R}{A square positive-definite matrix representing the scale matrix for the Wishart prior on the precision matrix.}

\item{alpha.mean}{Numeric vector with as many elements as covariates in the model, indicating the mean of the asymmetric skewness parameters ($\alpha$).}

\item{alpha.prec}{Numeric vector with as many elements as covariates in the model, indicating the precision of the asymmetric skewness parameters ($\alpha$).}

\item{beta.mean}{A single numeric value indicating the mean of the global intercept.}

\item{beta.prec}{A single numeric positive value indicating the precision of the global intercept.}
}
\description{
Identify the correct structure to specify the priors for an asymmetric ellipsoid model using the local background covariance approach.
}
\examples{
priors <- mahalLocalPriorsAsym(cent.mean = rep(0, 4),
                               cent.prec = rep(1.0E-4, 4),
                               mu.b.mean = rep(0, 4),
                               mu.b.prec = rep(1.0E-4, 4),
                               R = diag(4),
                               alpha.mean = rep(0, 4),
                               alpha.prec = rep(0.1, 4),
                               beta.mean = 0,
                               beta.prec = 1.0E-4)
}
