% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mahalLocallocalPriors.R
\name{mahalLocallocalPriors}
\alias{mahalLocallocalPriors}
\title{Learn how to set up priors for a Mahalanobis distance point process model with climate covariance matrix caculated from the poisson likelihood}
\usage{
mahalLocallocalPriors(
  mu.b.mean = NULL,
  mu.b.prec = NULL,
  R = NULL,
  beta.mean = NULL,
  beta.prec = NULL
)
}
\arguments{
\item{mu.b.mean}{Numeric, with as many elements as covariates in the ppmve model. Must be specified in the same order they appear in "covariate.names".
Each value indicates the likely niche centroid coordinates.}

\item{mu.b.prec}{Numeric, with as many elements as covariates in the ppmve model. Must be specified in the same order they appear in "covariate.names".
Each value indicates the precision (1/variance) of the niche centroid coordinates.}

\item{R}{A square matrix array, with rows and columns equal to the length of covariate names, and must be positive definite. 
may be generated by inverting a covariance matrix.}

\item{beta.mean}{A single numeric positive or negative value, inticates the mean of the global intercept.}

\item{beta.prec}{A single numeric positive, inticates the precision of the global intercept.}
}
\description{
Identify the correct structure to specify the priors for a euclidean distance model
}
\examples{
\dontrun{
priors <- mahalLocallocalPriors(mu.b.mean = rep(0, 4),
                                mu.b.prec = rep(1.0E-4, 4),
                                R = diag(4),
                                beta.mean = 0,
                                beta.prec = 1.0E-4)
}
}
