% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priorsPpmve.R
\name{priors.ppmve}
\alias{priors.ppmve}
\title{Generate a list of priors for espuntnich models}
\usage{
\method{priors}{ppmve}(model = NULL, relax.precision = FALSE, relax.factor = NULL)
}
\arguments{
\item{model}{A model object of class ppmve or ppmveAsym}

\item{...}{parameters passed on to priors}
}
\value{
Returns a list with mean and precision for centroids, covariance matrices and intercept
}
\description{
Use a local type of CovMat ppmve or ppmveAsym model to set the prior values for a locallocal model
}
\examples{

r <- system.file("extdata", "ChelsaBio.tif", package = "espuntnich") |> terra::rast() |> scale()

p <- system.file("extdata", "pointsAsym.csv", package = "espuntnich") |> read.csv()

m <- ppmve(points = p,
           covariates = r,
           covariate.names = names(r),
           CovMat = "local",
           Distance = "mahalanobis",
           no.bkgd = 5000,
           niter = 10000,
           nthin = 9,
           nburnin = 1000,
           chains = 1)

pri <- priors(m)

}
